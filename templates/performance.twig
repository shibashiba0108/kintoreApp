<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="content-type" content="text/html; charset=utf-8"/>
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&display=swap">
		<link href="{{ constant('kintore\\contents\\Bootstrap::APP_URL') }}kintore/css/kintore.css" rel="stylesheet" type="text/css"/>
		<link rel="stylesheet" href="{{ constant('kintore\\contents\\Bootstrap::APP_URL') }}kintore/css/list.css" type="text/css"/>
		<link rel="stylesheet" href="{{ constant('kintore\\contents\\Bootstrap::APP_URL') }}kintore/css/detail.css" type="text/css"/>
		<link
		rel="stylesheet" href="{{ constant('kintore\\contents\\Bootstrap::APP_URL') }}kintore/css/performance.css" type="text/css"/>
	 <script src="{{ constant('kintore\\contents\\Bootstrap::APP_URL') }}kintore/js/main.js"></script>
	</head>
	<body>
		<div id="wrapper">
			{{ include('header2.twig') }}
			{% if dataArr is empty %}
				<p class="empty">まだ本日トレーニングをしていません</p>
			{% else %}
				<div id="excs_list">
					{% for value in dataArr %}
						<div class="today" id="performance-{{ value.id }}">
							<ul>
								<li class="name">{{ value.excs_name }}</li>
								<li>重さ:
									{{ value.weight_count }}
									kg</li>
								<li>回数:
									{{ value.rep_count }}</li>
								<li>セット数:
									{{ value.set_count }}</li>
								<li>所要時間:
									{{ value.duration }}
									分</li>
								<li>
									<button class="delete-button" data-id="{{ value.id }}">削除</button>
								</li>
							</ul>
						</div>
					{% endfor %}
				</div>
			{% endif %}
			<div id="back">
				<p>
					<a href="{{ constant('kintore\\contents\\Bootstrap::ENTRY_URL') }}training_history.php">全ての記録を見る</a>
					<a href="{{ constant('kintore\\contents\\Bootstrap::ENTRY_URL') }}list.php">種目一覧へ戻る</a><br><br>
					<a href="{{ constant('kintore\\contents\\Bootstrap::ENTRY_URL') }}mypage.php">マイページへ戻る</a><br><br>
				</p>
			</div>
		</div>
		{{ include('footer.twig') }}
	</body>
</html>
